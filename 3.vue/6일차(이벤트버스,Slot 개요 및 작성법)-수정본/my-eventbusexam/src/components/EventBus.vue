<script>
import Vue from "vue"; //import React from 'react'
//import ReactDOM ~(화면출력)
//eventBus라는 뷰객체를 생성
var eventBus = new Vue();
export default eventBus; //서로 다른 컴포넌트끼리 연결(통신(데이터전달))
</script>
